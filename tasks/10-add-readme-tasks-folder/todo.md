# #10 add-readme-tasks-folder

- [x] `tasks/README.md`, one line explaining the folder (edit only; PM commits it as `tasks:`) — dcal-builder
- [x] `verify-change` section 0 docs-only checks — dcal-verifier
- [ ] PR opened, checkout back on `master` (`new-feature` 6.2) — dcal-builder
